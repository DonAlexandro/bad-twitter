{{> messages}}
<div class="row">
    <div class="col-sm-12 col-md-10 col-lg-8 col-xl-6 mx-auto">
        <div class="card">
            <div class="card-body">
                <h5 class="mb-3">{{title}}</h5>
                {{> post-form data=post}}
            </div>
        </div>
    </div>
</div>
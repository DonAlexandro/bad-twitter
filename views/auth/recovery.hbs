{{> messages}}
<div class="card auth-card">
    <div class="card-body">
        <h5 class="mb-3">{{title}}</h5>
        <form action="/auth/recovery" method="POST">
            <input type="email" class="form-control" name="email" placeholder="Ел. адрес">
            <input type="hidden" name="_csrf" value="{{csrf}}">
            <button type="submit" class="btn btn-danger mt-3">Відновити</button>
        </form>
    </div>
</div>